/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'encrypted-tbn0.gstatic.com', 'lh3.googleusercontent.com'],
  },
};

export default nextConfig;
